import 'package:flutter/material.dart';

class AnalyzeHomePage extends StatelessWidget {
  const AnalyzeHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Analyze Page'),
    );
  }
}
